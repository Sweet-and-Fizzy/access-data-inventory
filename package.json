{
  "name": "access-data-inventory",
  "version": "1.0.0",
  "description": "ACCESS Data Inventory - documentation generator",
  "private": true,
  "scripts": {
    "generate": "python generate.py",
    "validate": "python generate.py --validate",
    "serve": "cd docs && bundle exec jekyll serve --port 8080",
    "preview": "python generate.py && cd docs && bundle exec jekyll serve --port 8080",
    "dbdocs:login": "dbdocs login",
    "dbdocs:build": "dbdocs build docs/inventory.dbml --project access-data-inventory"
  },
  "devDependencies": {
    "dbdocs": "^1.0.1"
  }
}
